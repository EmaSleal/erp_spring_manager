# ═══════════════════════════════════════════════════════════
# WhatsApp Meta Business API - Credenciales (TEMPLATE)
# ══════════════════════════════════════════════════════════
# 
# INSTRUCCIONES:
# 1. Copia este archivo como: .env.local
# 2. Rellena con tus credenciales reales de Meta
# 3. NO commitees el archivo .env.local (está en .gitignore)
# 
# ═══════════════════════════════════════════════════════════

# Phone Number ID (obtenido de Meta Developer Console)
# Ubicación: WhatsApp → API Setup → Phone number ID
META_WHATSAPP_PHONE_NUMBER_ID=tu_phone_number_id_aqui

# Access Token (generar en Meta Developer Console)
# Ubicación: WhatsApp → API Setup → Generate token
# IMPORTANTE: Token temporal (24 horas) - Regenerar diariamente
META_WHATSAPP_ACCESS_TOKEN=tu_access_token_aqui

# API Version (versión de la API de Meta)
META_WHATSAPP_API_VERSION=v18.0

# Base URL de la API de Meta
META_WHATSAPP_API_URL=https://graph.facebook.com

# Webhook Verify Token (token personalizado para verificación)
# Generar un token aleatorio seguro (mínimo 32 caracteres)
# Usar el mismo token en Meta Developer Console → Webhooks
META_WEBHOOK_VERIFY_TOKEN=tu_webhook_verify_token_aqui

# ═══════════════════════════════════════════════════════════
# INSTRUCCIONES DE USO EN DESARROLLO
# ═══════════════════════════════════════════════════════════
#
# PowerShell:
#   1. Cargar variables: . .\.env.ps1
#   2. Ejecutar app: mvn spring-boot:run
#
# CMD:
#   1. set META_WEBHOOK_VERIFY_TOKEN=valor
#   2. mvn spring-boot:run
#
# Linux/Mac:
#   1. export META_WEBHOOK_VERIFY_TOKEN=valor
#   2. mvn spring-boot:run
#
# ═══════════════════════════════════════════════════════════
